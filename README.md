# Pixel Pac

An 8-bit Pac-Man-inspired browser game with custom character identities.

## Local Development

Install dependencies:

```sh
npm install
```

Run the Phaser game locally:

```sh
npm run dev
```

Build the static app:

```sh
npm run build
```

Run the gameplay validation checks:

```sh
npm test
```
